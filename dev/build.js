/*
    script to be run on build. this should be run with nodemon in repo whilst developing. also by pipeline prior to deploy
    primary purpose is to render out markdown books. rendered books are dumped into /books/rendered
    its purpose may grow as the library expands
*/